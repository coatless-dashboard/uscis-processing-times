# .github/workflows/render.yml
name: Render dashboard

on:
  # after a data update lands, so the site always reflects the newest panel
  workflow_run:
    workflows: ["Update"]
    types: [completed]
  # editing the dashboard itself
  push:
    paths:
      - "site/**"
      - ".github/workflows/render.yml"
  # and on demand
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: processing-times-render
  cancel-in-progress: true

jobs:
  render:
    # a failed data update should not publish a stale or half-built panel
    if: >-
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    outputs:
      rendered: ${{ steps.panel.outputs.found }}
    steps:
      - uses: actions/checkout@v7

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: "release"
          use-public-rspm: true
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::DBI
            any::RSQLite
            any::dplyr
            any::tidyr
            any::ggplot2
            any::htmltools
      - uses: quarto-dev/quarto-actions/setup@v2

      # Until the update workflow has run once there is no panel to render.
      # That is a precondition, not a fault, so report it and stop rather than
      # failing the run and leaving a red badge on a healthy repository.
      - name: Fetch the published panel
        id: panel
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -uo pipefail
          mkdir -p panel
          if gh release download current --pattern "processing-times.db" --dir panel 2>/dev/null; then
            echo "found=true" >> "$GITHUB_OUTPUT"
            ls -lh panel/
          else
            echo "found=false" >> "$GITHUB_OUTPUT"
            echo "::notice title=Nothing to render::No panel has been published yet. Run the Update workflow first; it creates the rolling current release this job reads."
          fi

      - name: Render
        if: steps.panel.outputs.found == 'true'
        env:
          # the panel carries the office dimension, so nothing else is needed
          USCIS_PANEL: ${{ github.workspace }}/panel/processing-times.db
        run: quarto render site/index.qmd --to dashboard

      - name: Fail if the build produced an empty dashboard
        if: steps.panel.outputs.found == 'true'
        run: |
          set -euo pipefail
          test -s site/index.html
          grep -q "quarto-dashboard" site/index.html
          if grep -q "observablehq--error" site/index.html; then
            echo "dashboard rendered with OJS errors"; exit 1
          fi

      - name: Collect only the rendered output
        if: steps.panel.outputs.found == 'true'
        run: |
          set -euo pipefail
          mkdir -p _publish
          cp site/index.html _publish/
          cp -R site/index_files _publish/
          # the .qmd, the scss and any local env file must not reach Pages
          find _publish -name "*.qmd" -o -name ".Renviron" -o -name "*.scss" | tee /dev/stderr | grep -q . && exit 1 || true

      - uses: actions/upload-pages-artifact@v3
        if: steps.panel.outputs.found == 'true'
        with:
          path: _publish

  deploy:
    needs: render
    # Pages needs a public repository on a free plan, and there is nothing to
    # deploy until a panel exists.
    if: needs.render.outputs.rendered == 'true'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
